import React from "react";

const DescFile = ({ image }) => {
  return (
    <div>
      <h2 style={{fontWeight:'bold'}}>Lorem ipsum dolor sit amet</h2>
      <p>
        <img src={image} style={{ width: 90, height: 100, marginTop: 10 }} />
        Carol a lot of text now a lot of text now a lot of text now a lot of
        text now a lot of text now a lot of text now a lot of text now a lot of
        text now a lot of text now a lot of text now a lot of text now a lot of
        text now a lot of text now a lot of text now a lot of text now a lot of
        text now a lot of text now a lot of text now a lot of text now a lot of
        text now a lot of text now a lot of text now a lot of text now a lot of
        text now a lot of text now a lot of text now a lot of text now a lot of
        text now a lot of text now a lot of text now{" "}
      </p>
    </div>
  );
};

export default DescFile;
